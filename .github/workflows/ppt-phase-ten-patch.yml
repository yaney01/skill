name: Apply phase ten overview assertion patch

on:
  push:
    branches:
      - agent/ppt-phase-ten-editor-enhancements

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ppt-phase-ten-editor-enhancements
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node ppt/scripts/patch-phase-ten-overview-test.mjs
      - run: |
          if git diff --quiet; then exit 0; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ppt/tests/editor.test.mjs
          git commit -m "Check presenter overview DOM without matching runtime source"
          git push origin HEAD:agent/ppt-phase-ten-editor-enhancements
