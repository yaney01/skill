# Test fixtures

Most invalid decks are derived from the real regression example inside a temporary directory. This keeps fixtures aligned with the current markup and avoids maintaining duplicate runtime copies. Permanent binary or edge-case fixtures may be added here when a test cannot be expressed as a deterministic transformation.
